<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /foodphoria/

    # Jika file atau folder ada, jangan rewrite
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Selain itu, lempar ke index.php
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
